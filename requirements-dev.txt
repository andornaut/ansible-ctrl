# The lint gate's own tooling, pinned so a release upstream cannot turn a check
# red without a commit here. Dependabot proposes the bump through the pip
# ecosystem.
ansible-lint==26.8.0
# A dependency of ansible-lint rather than a tool installed on its own, pinned
# because the config check runs its ansible-config, and ansible-lint runs its
# ansible-playbook: without this a core release moves under the gate while
# ansible-lint stays put.
ansible-core==2.21.3
# The Python gate, the same pin the other Python repositories here carry so one
# version decides what is rejected everywhere.
ruff==0.16.3
# The shell gate. Every other repository here pins the same ShellCheck through
# the action's version input, which Dependabot cannot read; the wheel carries
# the binary, so pinning it here puts the version under the same manifest as the
# rest and leaves a local run checking against what the gate checks against.
shellcheck-py==0.11.0.1
