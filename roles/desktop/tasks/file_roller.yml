---
# Default archive handler for desktop_user; formats in
# desktop_file_roller_mime_types. mimeapps.list is written directly rather than
# via xdg-mime, which resolves its path from $HOME.
- name: Set file-roller as the default archive handler
  become: true
  become_user: "{{ desktop_user }}"
  community.general.ini_file:
    path: "{{ desktop_user_home }}/.config/mimeapps.list"
    section: Default Applications
    option: "{{ item }}"
    value: org.gnome.FileRoller.desktop
    no_extra_spaces: true
    mode: "0644"
  loop: "{{ desktop_file_roller_mime_types }}"
