---
- name: Install system packages
  ansible.builtin.apt:
    name:
      - adb
      - gh
      - git
      - git-delta
      - git-filter-repo
      - jq
      - libmysqlclient-dev
      - libxml2-dev
      - libxslt-dev
      - meld
      - mercurial
      - mysql-client
      - postgresql-client
      - shellcheck
      - snmp
      - wireshark
      - xserver-xephyr
    state: latest
    install_recommends: false
    update_cache: true
  become: true
