---
galaxy_info:
  author: andornaut
  description: Install gaming applications and utilities on Ubuntu
  license: MIT
  min_ansible_version: "2.19"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - games
    - gaming
    - steam
    - ubuntu

# None. The desktop role installs flatpak too, but adds the remote for desktop_user,
# and per-user state is not shared, so flatpak.yml installs flatpak and adds the remote
# itself, for games_user.
