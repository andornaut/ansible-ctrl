---
galaxy_info:
  author: andornaut
  description: Install gaming applications and utilities on Ubuntu
  license: MIT
  min_ansible_version: "2.17"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - games
    - gaming
    - steam
    - ubuntu

# No dependencies. The desktop role also installs flatpak and adds the flathub remote,
# but it adds the remote for desktop_user, and flatpak's per-user state is not shared
# between accounts, so flatpak.yml bootstraps both for games_user itself.
