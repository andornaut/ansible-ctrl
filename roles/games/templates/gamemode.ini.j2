# {{ ansible_managed }}
#
# Read after /usr/share/gamemode/gamemode.ini and before the launching account's own
# ~/.config/gamemode.ini, which overrides anything set here. Only the settings this role
# owns appear; the rest stay at the shipped defaults.
[general]
inhibit_screensaver={{ games_gamemode_inhibit_screensaver | ternary(1, 0) }}
