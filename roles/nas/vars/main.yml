---
# Derived from defaults/main.yml, not operator knobs. Role vars outrank host_vars, so
# a host cannot desync one from its source.

# From the getent in tasks/main.yml. Override nas_key_file to move the LUKS key.
nas_user_home: "{{ ansible_facts['getent_passwd'][nas_user][4] }}"
