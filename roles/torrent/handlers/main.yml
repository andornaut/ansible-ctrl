---
# No daemon_reload: main.yml's "Enable and start" does it, before handlers fire.
- name: Restart rtorrent
  ansible.builtin.systemd_service:
    name: rtorrent.service
    state: restarted
  become: true
